MODULE main
VAR
  p : boolean;
  q : boolean;
ASSIGN
  next(p) := case
    p : TRUE;
    TRUE : {TRUE,FALSE};
  esac;
  next(q) := case
    q : TRUE;
    TRUE : {TRUE,FALSE};
  esac;

SPEC (EF p & EF q ) -> EG (p&q)
SPEC (AF p | AF q) -> AF (p|q)
SPEC AF (p|q) -> (AF p | AF q)